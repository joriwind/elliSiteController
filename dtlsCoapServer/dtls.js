var ffi = require('ffi');

var dtls = function(){
   
}



var dtls-interface = ffi.Library('dtls-interface', {
  'init': [ 'double', [ 'double' ] ],
  '
});


dtls.prototype.createServer(options, callback){
   
}